import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 200, // Ajusta esta altura según las necesidades de tu diseño
        width: '100%',
      },
      webView: {
        flex: 1,
      },
});