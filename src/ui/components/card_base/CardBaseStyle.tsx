import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
      // Estilos para tus cards
    },
    cardImage: {
      width: '100%',
      height: 200,
      // Estilos para la imagen
    },
    cardTitle: {
      // Estilos para el título
    },
    // Estilos adicionales para rating, etc.
  });
  